# Generate secure tokens: openssl rand -hex 32
VAULT_SYNC_TOKEN=your-secret-token-here

# MCP Server token (REQUIRED for MCP endpoint)
# This is a separate token from VAULT_SYNC_TOKEN for security
VAULT_SYNC_MCP_TOKEN=your-mcp-token-here
