# Edge-токен периметра. РЕАЛЬНЫЙ файл 00-edge-token.conf — СЕКРЕТ, НЕ в git (.gitignore).
# Грузится в http-контекст раньше vault-sync.conf (алфавитный порядок conf.d).
# X-Auth-Token == токен → $edge_ok=1, иначе 0 → 401 → fail2ban.
# Токен = VAULT_SYNC_TOKEN (тот же, что шлёт Obsidian-плагин).
#
# На VPS: cp 00-edge-token.conf.example 00-edge-token.conf, вписать токен,
#         docker compose up -d --force-recreate nginx
map_hash_bucket_size 128;
map $http_x_auth_token $edge_ok {
    default 0;
    "<VAULT_SYNC_TOKEN>" 1;
}
