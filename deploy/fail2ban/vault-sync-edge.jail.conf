# /etc/fail2ban/jail.d/vault-sync-edge.conf — ставит install.sh (__REPO__ → путь к репо)
[vault-sync-edge]
enabled  = true
port     = http,https
filter   = vault-sync-edge
logpath  = __REPO__/deploy/nginx/log/access.log
maxretry = 3
findtime = 10m
bantime  = 1h
