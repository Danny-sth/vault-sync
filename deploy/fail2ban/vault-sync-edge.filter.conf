# /etc/fail2ban/filter.d/vault-sync-edge.conf
# Бан IP, который бьёт по эндпоинтам без валидного edge-токена (nginx → 401).
[Definition]
failregex = ^<HOST> .* "(GET|POST|PUT|DELETE|HEAD|PATCH) [^"]+ HTTP/[0-9.]+" 401 
ignoreregex =
